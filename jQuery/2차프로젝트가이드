1. AJAX와 OpenAPI 사용은 필수 (취업을 위해서)
2. 게시판에 검색(서치) 기능을 넣는 것도 도움이 됨(1차에 넣지 않았던 사람들)
3. 답변형 게시판도 좋은 점수, 답변에 답변에 답변 끊임없이 달면 모양이 나빠짐
4. 데이터 시각화(차트) 부분이 들어가도록

VO(Value Object), DTO(Data Transfer Object)  데이터가 보이오
DAO(Data Access Object) 데이터를 다오

 VO는 어떤 기준으로 만드는지
 일반적으로 DB 테이블의 1개 row에 해당하는 데이터를 vo에 담을 수 있도록 vo 설계
 vo를 collection(ArrayList나 HashMap)에 담아서 많이 사용 -> resultset : 메모리에 있으므로 빨리 가져다 쓸수 있음
 DB 커넥션은 시간과 비용이 많이 드는 작업임
 DB 커넥션 관리의 효율을 높이기위해
 DB 커넥션을 미리 만들어 놓는게 커넥션 풀(성능향상) : 사용자에게 커넥션을 할당함

